<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">
  <springProperty scope="context" name="LOG_PATH" source="logging.file.path" defaultValue="logs"/>
  <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="app"/>

  <property name="LOG_PATTERN_MDC"
            value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg rid=%X{requestId} uid=%X{userId}%n"/>

  <!-- Console -->
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder><pattern>${LOG_PATTERN_MDC}</pattern></encoder>
  </appender>

  <!-- app 主檔（只給你自家 package 用） -->
  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_PATH}/${APP_NAME}.log</file>
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>${LOG_PATH}/${APP_NAME}-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
      <maxFileSize>50MB</maxFileSize>
      <maxHistory>30</maxHistory>
      <cleanHistoryOnStart>true</cleanHistoryOnStart>
    </rollingPolicy>
    <encoder><pattern>${LOG_PATTERN_MDC}</pattern></encoder>
  </appender>

  <!-- 全域錯誤專檔（任何套件的 ERROR 都會進來） -->
  <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${LOG_PATH}/${APP_NAME}-error.log</file>
    <filter class="ch.qos.logback.classic.filter.ThresholdFilter"><level>ERROR</level></filter>
    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>${LOG_PATH}/${APP_NAME}-error-%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
      <maxFileSize>20MB</maxFileSize>
      <maxHistory>60</maxHistory>
    </rollingPolicy>
    <encoder><pattern>${LOG_PATTERN_MDC}</pattern></encoder>
  </appender>

  <!-- 非同步包裝 -->
  <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
    <appender-ref ref="FILE"/><queueSize>8192</queueSize><neverBlock>true</neverBlock>
  </appender>
  <appender name="ASYNC_ERROR" class="ch.qos.logback.classic.AsyncAppender">
    <appender-ref ref="ERROR_FILE"/><queueSize>4096</queueSize><neverBlock>true</neverBlock>
  </appender>

  <!-- ✅ 根 logger：只輸出到 console + 全域錯誤檔；不寫一般檔案 -->
  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="ASYNC_ERROR"/>
  </root>

  <!-- ✅ 只把你自家 package（調整成你的 base package，例如 com.EEITG3）寫進檔案 -->
  <logger name="com.EEITG3" level="INFO" additivity="false">
    <!-- additivity=false：避免再往上送到 root，造成重複 -->
    <appender-ref ref="CONSOLE"/>
    <appender-ref ref="ASYNC_FILE"/>
    <appender-ref ref="ASYNC_ERROR"/>
  </logger>

  <!-- 可選：降低框架噪音 -->
  <logger name="org.springframework" level="INFO"/>
  <logger name="org.hibernate.SQL" level="WARN"/>
  <logger name="org.apache.catalina" level="INFO"/>
</configuration>